<claude-mem-context>
# Memory Context

# [guizang-test] recent context, 2026-05-20 7:40pm GMT+8

No previous sessions found.
</claude-mem-context>

# Project Defaults

- 执行 implementation plan 时，默认使用 `Subagent-Driven`，除非用户明确指定其他方式。
- 需要创建 git worktree 时，默认使用仓库内的 `.worktrees/` 目录，不再重复询问。
